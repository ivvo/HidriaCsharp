namespace ToolblockManager
{
    /// <summary>
    /// This structure represents different name types for manager methods.
    /// </summary>
    public enum NameType
    {
        FullName,
        Name,
    }

    /// <summary>
    /// This structure represents toolblock types.
    /// </summary>
    public enum ToolblockType
    {
        Normal,
        Calibration,
    }
}
