<views:MvxWpfView
    xmlns:views="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf"
    xmlns:mvx="clr-namespace:MvvmCross.Platforms.Wpf.Binding;assembly=MvvmCross.Platforms.Wpf"
    x:Class="TemplateGenerator.Wpf.Views.ShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TemplateGenerator.Wpf.Views"
             mc:Ignorable="d" FontSize="20" 
             d:DesignHeight="550" d:DesignWidth="1300" Background="AliceBlue">
    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="20"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <!--Row 0 - obstoječi projekt-->
        <Button Click="ImportProjectClick" Grid.Row="0" Grid.Column="1" MinWidth="120" Margin="5" Content="Import Project"/>

        <Button Click="UpdateProjectClick" Grid.Row="0" Grid.Column="2" MinWidth="120" Margin="5" Content="Update Project"/>

        <!--Row 1-->
        <TextBox Text="{Binding ProgramName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="1" Margin="5"  Height="30" MinWidth="100">
            <TextBox.InputBindings>
                <KeyBinding Key="Enter" Command="{Binding AddProgramCommand}"/>
            </TextBox.InputBindings>
        </TextBox>

        <TextBlock Grid.Row="1" Grid.Column="2"  Margin="5" Height="30" HorizontalAlignment="Center" 
                       VerticalAlignment="Center"><Run Text="Program name"/></TextBlock>

        <Button mvx:Bi.nd="Command AddProgramCommand" Grid.Row="1"  Grid.Column="3" MinWidth="120" Margin="5"
                Content="Add Program/Robot"/>

        <Button mvx:Bi.nd="Command RemoveProgramCommand"  Grid.Row="1" Grid.Column="4" MinWidth="70" Margin="5"  
                IsEnabled="{Binding CanClickRemoveProgram}" Content="Remove Robot/Program"/>

        <!--Označenega shranimo v SelectedRobot, prikažemo pa RobotName-->
        <ComboBox Grid.Row="1" Grid.Column="5" Margin="5" x:Name="Robot" MinWidth="150" 
                  SelectedItem="{Binding SelectedProgram, Mode=TwoWay}"     
                  DisplayMemberPath="ProgramName" 
                  ItemsSource="{Binding Program}"/>

        <!--Row 2-->
        <TextBox Text="{Binding StationName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" Grid.Column="1" Margin="5"  Height="30" MinWidth="100">
            <TextBox.InputBindings>
                <KeyBinding Key="Enter" Command="{Binding AddStationCommand}"/>
            </TextBox.InputBindings>
        </TextBox>

        <CheckBox  IsChecked="{Binding StationFreeEnabled}"  Grid.Row="2" Grid.Column="2" HorizontalAlignment="Center" VerticalAlignment="Center" Content="Station free" />

        <Button mvx:Bi.nd="Command AddStationCommand" Grid.Row="2" Grid.Column="3" MinWidth="70" Margin="5" Content="Add Station"/>

        <Button mvx:Bi.nd="Command RemoveStationCommand" Grid.Row="2" Grid.Column="4" MinWidth="150" Margin="5" Content="Remove Station"/>

        <!--Row 3-->
        <TextBlock Grid.Row="3" Grid.Column="1" Margin="3" Height="30" HorizontalAlignment="Center" 
                       VerticalAlignment="Center"><Run Text="Select template"/></TextBlock>

        <ComboBox ItemsSource="{Binding Templates}" Grid.Row="3" Grid.Column="2" Margin="5,5,5,0" Height="30" 
                  VerticalAlignment="Top" SelectedItem="{Binding SelectedTemplate}">
        </ComboBox>

        <Button Click="GenerateClick" Grid.Row="3" Grid.Column="3" Margin="5" Content="Generate" />

        <CheckBox x:Name="GenerateSimpleProgram" Grid.Row="3" Grid.Column="4" Content="Generate Simple Program" VerticalAlignment="Center" IsChecked="True"></CheckBox>

        <Button mvx:Bi.nd="Command TestButtonCommand" Grid.Row="3" Grid.Column="5" Content="Test" Margin="5"/>

        <!--Row 4-->
        <DataGrid ItemsSource="{Binding Path=SelectedProgram.Stations}" Grid.Row="4" Grid.Column="1" MinWidth="300" Margin="5" Grid.RowSpan="6" Grid.ColumnSpan="5"
                AutoGenerateColumns="True"></DataGrid>

        <!-- Last Row 10-->
        <TextBlock Grid.Row="10" Grid.Column="1" MinWidth="100" Margin="5" Grid.ColumnSpan="3" Text="{Binding TextUpdate, Mode=OneWay}"/>
    </Grid>
</views:MvxWpfView>
